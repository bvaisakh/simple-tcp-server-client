# simple-tcp-server-client

## Building

```bash
mkdir build
cd build
cmake -S .. -B .
cmake --build .
```

## Running

```bash
# From inside the build folder

# Starting the server
./server

# Starting the client
./client localhost
```
